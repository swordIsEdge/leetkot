package middle

object LongestSubstring_395 {
    fun longestSubstring(s: String, k: Int): Int {
        if(s.isEmpty() || s.length<k) return 0
        val counter = IntArray(26)
        val set = hashSetOf<Char>()
TODO()

    }
}